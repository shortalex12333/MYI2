---
phase: 01-foundation
plan: 04
type: execute
wave: 1
depends_on: []
files_modified:
  - src/lib/schema/faq-schema.ts
  - src/lib/schema/breadcrumb-schema.ts
  - src/lib/schema/index.ts
  - src/app/qa/[id]/page.tsx
autonomous: true
requirements:
  - FR-5.1
  - FR-5.2
  - FR-5.3
  - FR-5.4

must_haves:
  truths:
    - "FAQPage schema renders on Q&A content pages"
    - "BreadcrumbList schema renders on content pages"
    - "Article schema already exists on papers pages (verify)"
    - "Organization schema already exists site-wide (verify)"
  artifacts:
    - path: "src/lib/schema/faq-schema.ts"
      provides: "FAQPage JSON-LD generation"
      exports: ["generateFAQSchema"]
    - path: "src/lib/schema/breadcrumb-schema.ts"
      provides: "BreadcrumbList JSON-LD generation"
      exports: ["generateBreadcrumbSchema"]
    - path: "src/lib/schema/index.ts"
      provides: "Schema module exports"
      exports: ["generateFAQSchema", "generateBreadcrumbSchema"]
  key_links:
    - from: "src/app/qa/[id]/page.tsx"
      to: "src/lib/schema/faq-schema.ts"
      via: "import"
      pattern: "from '@/lib/schema'"
    - from: "Q&A pages"
      to: "Schema.org"
      via: "JSON-LD script tag"
      pattern: "application/ld\\+json"
---

<objective>
Implement Schema.org structured data generators for FAQPage and BreadcrumbList, and integrate into Q&A content pages.

Purpose: Schema.org markup is critical for AI citations. 96% of AI citations come from sites with proper structured data. FAQPage schema helps Q&A content appear in search features and be cited by AI systems.

Output: Schema generation utilities and Q&A page with FAQPage markup.
</objective>

<execution_context>
@/Users/celeste7/.claude/get-shit-done/workflows/execute-plan.md
@/Users/celeste7/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/01-foundation/01-RESEARCH.md

# Existing Schema.org patterns
@src/app/layout.tsx (Organization, Person, WebSite schemas)
@src/app/papers/[slug]/page.tsx (Article schema)
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create FAQPage schema generator</name>
  <files>src/lib/schema/faq-schema.ts</files>
  <action>
Create FAQPage Schema.org generator following the pattern from research.

The generator should:
1. Export `generateFAQSchema(question: string, answer: string)` for single Q&A
2. Export `generateMultiFAQSchema(items: FAQItem[])` for multiple Q&As
3. Return proper FAQPage JSON-LD structure with @context, @type, mainEntity
4. Use Schema.org spec: https://schema.org/FAQPage

Interface:
- FAQItem: { question: string; answer: string }
- FAQPageSchema: Schema.org FAQPage structure

The schema output should match the research section "Schema.org FAQPage Implementation" pattern exactly.

Note: JSON-LD for Schema.org structured data is safe to render via script tags as it contains only serialized JSON from our own data - this is the standard Next.js pattern for structured data as shown in existing layout.tsx and papers/[slug]/page.tsx.
  </action>
  <verify>
1. File exists at src/lib/schema/faq-schema.ts
2. Exports generateFAQSchema and generateMultiFAQSchema
3. Schema structure matches Schema.org FAQPage spec
4. Includes @context, @type, mainEntity with Question/Answer
  </verify>
  <done>
FAQPage schema generator created with support for single and multiple Q&A items.
  </done>
</task>

<task type="auto">
  <name>Task 2: Create BreadcrumbList schema generator</name>
  <files>src/lib/schema/breadcrumb-schema.ts</files>
  <action>
Create BreadcrumbList Schema.org generator.

The generator should:
1. Export `generateBreadcrumbSchema(items: BreadcrumbItem[])`
2. Export `COMMON_BREADCRUMBS` object with pre-defined patterns for papers, qa, topics
3. Use BASE_URL = 'https://www.myyachtsinsurance.com'
4. Return proper BreadcrumbList JSON-LD structure

Interface:
- BreadcrumbItem: { name: string; url: string }
- BreadcrumbListSchema: Schema.org BreadcrumbList structure

Each item in itemListElement needs:
- @type: 'ListItem'
- position: 1-indexed position
- name: breadcrumb text
- item: full URL

This follows the exact structure from research section "BreadcrumbList Schema Implementation".
  </action>
  <verify>
1. File exists at src/lib/schema/breadcrumb-schema.ts
2. Exports generateBreadcrumbSchema and COMMON_BREADCRUMBS
3. Schema structure matches Schema.org BreadcrumbList spec
4. URLs are prefixed with BASE_URL
  </verify>
  <done>
BreadcrumbList schema generator created with common patterns for papers, Q&A, and topics.
  </done>
</task>

<task type="auto">
  <name>Task 3: Create schema module index and integrate FAQPage into Q&A page</name>
  <files>
    src/lib/schema/index.ts
    src/app/qa/[id]/page.tsx
  </files>
  <action>
First, create the schema module index that re-exports all schema functions and types:
- generateFAQSchema, generateMultiFAQSchema, FAQItem, FAQPageSchema
- generateBreadcrumbSchema, COMMON_BREADCRUMBS, BreadcrumbItem, BreadcrumbListSchema

Then, create/update Q&A detail page with FAQPage schema:

1. Check if Q&A page exists at src/app/qa/[id]/page.tsx or alternate location
2. If the app uses /knowledge route instead, adapt accordingly
3. Add FAQPage and BreadcrumbList schemas to the page

The page should:
- Fetch Q&A entry from qa_candidates table (published status)
- Generate FAQPage schema with question and answer
- Generate BreadcrumbList schema with Home > Q&A > [question]
- Include both schemas via script type="application/ld+json"
- Render breadcrumb navigation UI
- Display question as h1 and answer in prose styling

Note: JSON-LD structured data in script tags is the standard Next.js pattern for SEO (see existing patterns in layout.tsx and papers/[slug]/page.tsx). The JSON.stringify of our own schema objects is safe as we control the data source.

Also verify existing schemas (no changes needed):
- FR-5.2: Article schema in papers/[slug]/page.tsx (confirm exists)
- FR-5.3: Organization schema in layout.tsx (confirm exists)
  </action>
  <verify>
1. src/lib/schema/index.ts exports all schema generators
2. Q&A detail page exists (either /qa/[id] or adapted route)
3. Page includes FAQPage schema via script type="application/ld+json"
4. Page includes BreadcrumbList schema
5. Verify existing Article schema in papers/[slug]/page.tsx (FR-5.2)
6. Verify existing Organization schema in layout.tsx (FR-5.3)
  </verify>
  <done>
Schema module complete. FAQPage and BreadcrumbList integrated into Q&A pages. Existing Article and Organization schemas verified.
  </done>
</task>

</tasks>

<verification>
After all tasks complete:

1. **Schema files created:**
   - src/lib/schema/faq-schema.ts
   - src/lib/schema/breadcrumb-schema.ts
   - src/lib/schema/index.ts

2. **Schema markup renders:**
   - Visit Q&A detail page, view source shows FAQPage JSON-LD
   - View source shows BreadcrumbList JSON-LD

3. **Existing schemas verified:**
   - src/app/papers/[slug]/page.tsx has Article schema (FR-5.2)
   - src/app/layout.tsx has Organization schema (FR-5.3)

4. **Validation:**
   - Test JSON-LD with Google Rich Results Test
   - No syntax errors in schema output
</verification>

<success_criteria>
- [ ] src/lib/schema/faq-schema.ts exports generateFAQSchema
- [ ] src/lib/schema/breadcrumb-schema.ts exports generateBreadcrumbSchema
- [ ] src/lib/schema/index.ts re-exports all schema functions
- [ ] Q&A detail page includes FAQPage schema in script tag
- [ ] Q&A detail page includes BreadcrumbList schema in script tag
- [ ] Verified: Article schema exists in papers/[slug]/page.tsx
- [ ] Verified: Organization schema exists in layout.tsx
</success_criteria>

<output>
After completion, create `.planning/phases/01-foundation/01-04-SUMMARY.md`
</output>
